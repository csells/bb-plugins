{
  "name": "csells-bb-plugins",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "plugins/*"
  ],
  "scripts": {
    "build": "npm run build --workspace bb-plugin-project-files",
    "test": "npm run test --workspace bb-plugin-project-files",
    "typecheck": "npm run typecheck --workspace bb-plugin-project-files"
  }
}
